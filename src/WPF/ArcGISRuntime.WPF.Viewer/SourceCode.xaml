<UserControl x:Class="ArcGISRuntime.WPF.Viewer.SourceCode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:ArcGISRuntime.Samples.Shared.Models"
             mc:Ignorable="d"
             Background="{StaticResource PrimaryBackgroundColor}"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="0" Margin="5" Click="LoadCsharp" x:Name="CsharpButton" IsEnabled="False">C# Source Code</Button>
        <Button Grid.Row="0" Grid.Column="1" Margin="5" Click="LoadXml"  x:Name="XmlButton"  IsEnabled="True">XAML Source Code</Button>
        <WebBrowser x:Name="sourceCodeBrowser" Width="auto" Height="auto" Grid.Row="1" Grid.ColumnSpan="2"/>
    </Grid>
</UserControl>